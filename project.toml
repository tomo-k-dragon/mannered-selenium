[build-system]
requires = ["setuptools>=65.5.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mannered-selenium"
version = "0.1.0"
description = "Polite web scraping using Selenium WebDriver following robots.txt"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "selenium>=4.15.0"
]
authors = [
    { name="tomo-k-dragon", email="tomokdragon@gmail.com" }
]
license = { text="MIT" }
homepage = "https://github.com/tomo-k-dragon/mannered-selenium"